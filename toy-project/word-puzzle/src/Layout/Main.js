import { Link } from "react-router-dom";

const Main = () => {
  return (
    <Link to="/select">
      <button>
        <span>시작</span>
      </button>
    </Link>
  );
};

export default Main;
